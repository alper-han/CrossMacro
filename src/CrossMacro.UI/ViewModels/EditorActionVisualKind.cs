namespace CrossMacro.UI.ViewModels;

/// <summary>
/// UI-only visual grouping for editor action list rows.
/// </summary>
public enum EditorActionVisualKind
{
    Noise,
    Movement,
    Pointer,
    Keyboard,
    Text,
    Timing,
    ControlFlow,
    Variable,
    Raw
}
