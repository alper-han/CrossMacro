
namespace CrossMacro.Cli.Tests;

public sealed partial class CliCommandRouterTests
{

















































































































    private static class CliCommandRouterAccessor
    {
        public static CliParseResult Parse(string[] args) => CliCommandRouter.Parse(args);

        public static string GetUsage(string? topic = null) => CliCommandRouter.GetUsage(topic);
    }
}
